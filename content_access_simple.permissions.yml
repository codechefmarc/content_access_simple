grant content access simple:
  title: 'Allow content access simple for all'
  description: 'Allow changing view permissions for all content types configured with per-node content access'

grant own content access simple:
  title: 'Allow content access simple for own'
  description: 'Allow changing view permissions for own nodes configured with per-node content access'
